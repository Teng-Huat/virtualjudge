<h2>New contest</h2>

<%= render "form.html", changeset: @changeset,
                        contest: nil,
                        current_user: @current_user,
                        action: admin_contest_path(@conn, :create) %>

                        <%= link "Back", to: admin_contest_path(@conn, :index) %>
